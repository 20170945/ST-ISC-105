#include <stdio.h>
#include <stdlib.h>
#include "heap.h"

void Encolar()
float DecolarCualquiera(Heap* Gatos, Heap*Perros)
float DecolarPerro(Heap* Gatos);
float DecolarGato(Heap* Gatos);


int main()
{
    Heap* Gatos = Inicializar(0);
    Heap* Perros = Inicializar(0);
    return 0;
}
